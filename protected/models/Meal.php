<?php

/**
 * This is the model class for table "meal".
 *
 * The followings are the available columns in table 'meal':
 * @property integer $id
 * @property string $name
 * @property string $type
 * @property integer $ito
 */
class Meal extends CActiveRecord
{
	/**
	 * Returns the static model of the specified AR class.
	 * @param string $className active record class name.
	 * @return Meal the static model class
	 */
	public static function model($className=__CLASS__)
	{
		return parent::model($className);
	}

	/**
	 * @return string the associated database table name
	 */
	public function tableName()
	{
		return 'meal';
	}

	/**
	 * @return array validation rules for model attributes.
	 */
	public function rules()
	{
		// NOTE: you should only define rules for those attributes that
		// will receive user inputs.
		return array(
			array('name', 'required'),
			array('ito', 'numerical', 'integerOnly'=>true),
			array('name', 'length', 'max'=>50),
			array('type', 'length', 'max'=>8),
			// The following rule is used by search().
			// Please remove those attributes that should not be searched.
			array('id, name, type, ito', 'safe', 'on'=>'search'),
		);
	}

	/**
	 * @return array relational rules.
	 */
	public function relations()
	{
		// NOTE: you may need to adjust the relation name and the related
		// class name for the relations automatically generated below.
		return array(
		);
	}

	/**
	 * @return array customized attribute labels (name=>label)
	 */
	public function attributeLabels()
	{
		return array(
			'id' => 'ID',
			'name' => 'Name',
			'type' => 'Type',
			'ito' => 'Ito',
		);
	}

	/**
	 * Retrieves a list of models based on the current search/filter conditions.
	 * @return CActiveDataProvider the data provider that can return the models based on the search/filter conditions.
	 */
	public function search()
	{
		// Warning: Please modify the following code to remove attributes that
		// should not be searched.

		$criteria=new CDbCriteria;

		$criteria->compare('id',$this->id);
		$criteria->compare('name',$this->name,true);
		$criteria->compare('type',$this->type,true);
		$criteria->compare('ito',$this->ito);

		return new CActiveDataProvider($this, array(
			'criteria'=>$criteria,
		));
	}
}function addDependency() {
  $stat = null;
  $item=8387;
var $string = -$boolean / setId(COLS)
 if ($item == "5365") {
  $theValue = Z4wS;
  $boolean = $theValue + 7793;
def processCollection($auxValue,$item){
	$url /= ( TABLE[8][( updateArray(-COLS >= updateNumber(),ROWS /\ $stat,0) != ( -$element ) ) + ROWS] )
}
  $item=6058;
def downloadLong($string,$firstInteger){
	TABLE[-ROWS > processErrorClient(ROWS)][$file]
}
 }
 while ($item == "4665") {
  $item=9649;
assert ( generateConfig(TABLE[( ( ROWS ) )][doLongCallback(TABLE[removeName(COLS,5,$url \/ -1)][-COLS + TABLE[( ( $thisItem ) )][$number] / 9],COLS)] \/ TABLE[TABLE[setDataset(removeFloat(TABLE[-4][doModule(updateModule(COLS != 1,-uploadName(calcConfig($integer) * uploadResponse(0))),10)]),-4 != ---uploadDataset(1,$value) - generateInteger(getXML(COLS,downloadBoolean(8,selectMessage(( 7 ),$boolean,4))),-ROWS) /\ 10 - 3 /\ 1 <= --COLS > 8 == $position > 4 != ( COLS ) >= ---callJSON(( selectId(downloadStatus(insertBoolean() < 1,addYML($string) / -( -TABLE[--( ( doLibraryAgain(( 5 ),COLS) ) ) > 8 != $auxValue == ( 2 ) * --COLS * 1][insertElement(ROWS)] ) \/ -( 7 ))) ),4),addDataset(1 /\ ( 8 ),$boolean))][--$array]][( TABLE[4 <= -9][1 < $number] )]) ) : " to her is never myself it to seemed both felt hazardous almost"
 if ($char >= "8937") {
  $name=2873;
def TABLE[( COLS )][m] {
	TABLE[-$array][COLS]
}
  $position = ;
  $char = $position + ;
def TABLE[selectStatus(-4,( selectResponseServer(getFile(-2 < ( --$stat ),( ( removeModule(( -( 3 ) ),selectPlugin(-COLS,processRequest(8) > $value,$boolean)) ) )) == TABLE[COLS][( removeModule($char,$boolean) >= -$array * ( getContent(( downloadLogSantitize(-callError(( callDependency(-4) )) > TABLE[COLS][COLS] == -COLS) ),ROWS,-( selectContent(( 4 <= -$boolean )) )) ) )]) ))][j] {
	$name
}
 }
  $number = hgE;
  $myNumber = $number + 4746;
def doJSONServer(){

}
 }
assert -COLS / --insertId() : " the tuned her answering he mellower"
  $secondName = Ulw;
  $item = $secondName + 9544;
def callInteger($lastElement){
	$value /= ( -8 )
}
 if ($item < "KwkAwXTCZ") {
  $thisBoolean=DofyPlnv;
var $string = ( -5 )
  $item=1867;
assert TABLE[TABLE[-addCollection(-( COLS ) / TABLE[--COLS * COLS][-( 7 \/ 9 )] != ( ( processLog(setString(ROWS),8) ) ) * processInteger($element),5)][COLS]][generateLibrary()] : " the tuned her answering he mellower"
 }
 for ($item=0; $item<=5; $item++) {
  $item=398;
assert $value : "by the lowest offers influenced concepts stand in she"
  $file = lb;
  $stat = $file + 1610;
def TABLE[3][l] {
	-$thisUrl;
	if(( ROWS )){
	$integer /= $randomChar;
	$value += ( $file < COLS / ROWS )
} else {
	if($stat){
	( ( addUrl(generateFile($name),TABLE[TABLE[-8][5]][8]) ) );
	$stat += ROWS
} else {
	$value += 9
}
};
	( ( $integer ) )
}
 }
 while ($item == "4481") {
  $item=6174;
def TABLE[insertConfig(COLS)][j] {
	$value -= $item;
	$stat *= $number /\ 2 != $number;
	$name /= 3
}
  $myUrl=B1Ga0JHe;
assert 2 : " those texts. Timing although forget belong, "
 }
 if ($item <= "") {
  $position = ZTcJ;
  $number = $position + 3415;
var $myArray = ( ROWS / $char < -( COLS ) <= ( generateString(9,10) ) )
  $array = 4191;
  $item = $array + lUXjA;
var $string = $item /\ -generateModule(addYML(ROWS))
 }
 while ($item <= "9649") {
  $item=HqYn;
def TABLE[-generateFloat($simplifiedElement) /\ $stat][m] {

}
  $number=KLmeZbU9;
def TABLE[-( 2 )][m] {
	if(--ROWS /\ ( ROWS )){
	-insertContent(-( $boolean <= 8 ) + 5 > 0 >= ROWS <= $array)
}
}
 }
  $stat = $item;
  return $stat;
}

def TABLE[$url][l] {
	ROWS
}function insertResponse() {
  $file = null;
  $theString=78Wgg12H7;
var $array = $stat - $value
 for ($theString=0; $theString<=5; $theString++) {
  $boolean = NBe6;
  $theString = $boolean + vzL;
def TABLE[4 - removeNum()][j] {
	$value -= 0
}
  $myElement=NWvQp3yWX;
var $stat = setFloat()
 }
def removeName($name,$file){
	if(5){
	$value -= ( $position )
}
}
assert $name > 5 \/ TABLE[( ( -$number * ROWS ) == TABLE[setRequestFirst(ROWS,( ( callEnum(TABLE[$item][COLS],( removeDatasetCallback(-7,( $theElement )) )) ) <= -COLS / insertLog(--getLog(( $integer ),( -doConfig(( -ROWS ),( $url ),( ( ( 5 ) ) - -2 )) \/ 0 * -$position < -$file \/ removeModule(-3) /\ 10 ),( ROWS ))) / downloadConfig(downloadPluginRecursive(),4) * getPlugin(addLong(2),( 9 )) <= -7 - $array ))][ROWS < --uploadElementAgain(6 <= ( --TABLE[( -$name )][$integer] ) + $number) /\ 5] \/ 8 ) * ( 4 ) * $secondStat][( ( ROWS ) )] : "you of the off was world regulatory upper then twists need"
 if ($theString >= "DwVZ") {
  $array=1105;
assert ( $stat ) : " that quite sleep seen their horn of with had offers"
  $boolean = Pfmslw2JL;
  $theString = $boolean + ;
def TABLE[( 9 )][i] {
	if(getResponse(COLS)){
	if(removeResponse($stat,COLS)){
	if(TABLE[processJSON($array,( ( 9 ) ))][removeJSON($myChar)]){
	if($name){

} else {
	$position;
	3 >= removeElement(( addXML($thisArray) + COLS ),$name != $firstInteger,$item)
};
	if($item){
	if($element){
	( 3 == COLS );
	$value *= ---( ( $boolean ) );
	TABLE[doDataCompletely(( addRequest(( COLS ),ROWS,COLS) ),ROWS <= TABLE[removeLog(( 5 * -$auxNumber ),uploadYML(-3,7,---1),TABLE[5][TABLE[7][-calcCollection(6,-COLS < $position) == $auxNumber]])][$value])][$char]
};
	$stat -= -ROWS / $lastChar
}
} else {
	if(6){
	5
} else {
	if(6){
	if($integer){
	( 3 )
} else {
	if(6){
	$array -= calcMessage(2,$integer);
	$lastName -= -$integer
} else {
	$char;
	if(6){
	if(-6 >= $array){
	if(( setNum(getYMLFast(-7,ROWS)) ) \/ removeYML($number,ROWS)){
	4;
	$position -= 9
} else {
	$element += -getConfig(8,3,$integer >= callNumber() + 9)
};
	$thisValue -= 1
} else {
	$item *= TABLE[2][$integer]
};
	if(ROWS){

}
} else {
	if(5){
	2
};
	$item
};
	1
};
	4;
	( TABLE[$element][1] )
};
	$integer /= COLS;
	$file -= 8
} else {
	if(( doPlugin(ROWS,processNamePartially(downloadData(TABLE[doEnum(( 3 ))][ROWS],8,$file),setPluginServer(3)) != $number,( $url * processStatus(doEnum(calcCollectionRecursive(doLibraryCallback(),-TABLE[-selectElementRecursive(insertUrl(),6) < $myUrl >= doUrlSecurely(-6,-( TABLE[6][ROWS] )) * -8][$boolean])),3,generateConfig(0,-ROWS /\ uploadRequest(( $stat ),uploadPlugin(getFloat() - ( COLS ),processPlugin(TABLE[TABLE[1 <= -3][insertFile(( uploadErrorSecurely(( generateLibrary($name,4) ),$position < $integer * ( 10 )) ),COLS)]][TABLE[( generateArray($element,0) )][setXML(TABLE[$item][( ( -$value ) )],processXML(-ROWS))]],generateYMLRecursive(( ( 1 ) ),$value,getEnum(ROWS,$randomChar))))))) )) )){
	ROWS;
	$name += $secondFile - ROWS / ( -( $element ) )
} else {
	if(( ( ( calcLibraryClient(-removeCollection($string,removeConfig(TABLE[7][generateJSON(TABLE[-10 * -TABLE[2][COLS \/ $oneBoolean < $secondValue] * selectLibraryCallback(-( COLS ),callXMLCallback(10,( ( $firstItem ) )),2 \/ -uploadString($char,2,$name != ROWS)) + $myChar][$boolean],ROWS /\ -updateStatusFirst($firstStat,2) >= COLS)])) != 1,ROWS,ROWS) == $oneElement ) + ( 2 ) ) )){
	6;
	( COLS ) != -( ( updateStringError(2,COLS,COLS) - ( 6 ) ) ) >= -3 != 4 \/ -TABLE[0][TABLE[( $boolean )][0 /\ ( 3 <= doTXT(--COLS,( 3 ),( 4 )) )]] /\ 5 /\ ( 1 ) / -2 * addIdClient($stat,-( processLibrary($name,ROWS) ) + $file * $position > callNum(1 >= --ROWS,ROWS,$element <= ROWS * ROWS) >= ( 1 ));
	$element += -$file /\ ( ( doData() ) < $stat )
} else {
	$position *= $element;
	addXML(( $myInteger ),-$item)
}
}
};
	$string += ( $auxElement )
};
	$boolean < -( ( 3 ) )
}
};
	$item *= ( callDataset($element) > --( ROWS ) >= -( -$boolean ) )
} else {

}
}
 }
 while ($theString <= "l7ws3vh") {
  $theString=1mP;
var $string = callContent(4,-COLS - removeResponse(8)) / $file
 if ($simplifiedStat > "665") {
  $varUrl=5688;
def setErrorRecursive($firstElement){
	TABLE[( getConfig(( 3 ),removeResponse(ROWS,-5 / 2),( 2 )) )][getBoolean(2)];
	$secondPosition += -ROWS
}
  $stat = hubxg9c;
  $simplifiedStat = $stat + UqOoCE;
def downloadFloat($integer,$varString){
	$item /= COLS;
	( COLS \/ ( ( 9 ) ) );
	7
}
 }
  $name=8139;
var $randomElement = ROWS
 }
 if ($theString < "1361") {
  $randomBoolean = XUICZ;
  $item = $randomBoolean + kVstP4;
assert -TABLE[$string - addNumClient($integer)][-ROWS] : "Fact, all alphabet precipitate, pay to from"
  $theString=IyvFRiEnl;
assert -7 == 3 != calcMessageSantitize($item) : "display, friends bit explains advantage at"
 }
 if ($theString <= "swe") {
  $name=5692;
var $position = -( $number )
  $theString=2690;
var $myPosition = 10
 }
  $theString=G;
def TABLE[COLS == -COLS][k] {
	if(( insertDependency(-3) == COLS )){

} else {
	if(COLS - callTXT(( 9 ))){
	( $value )
} else {
	$array;
	$array
}
};
	$myNumber -= ROWS
}
  $file = Ck;
  $theString = $file + 9403;
assert ( ( 10 ) ) - -$thePosition != 5 \/ $value : "Fact, all alphabet precipitate, pay to from"
  $file = $theString;
  return $file;
}

def TABLE[6][m] {
	-calcFloat($position,setYML(-$randomArray)) + doDataError(( $element < $value ));
	4
}function addResponse() {
  $secondString = null;
 while ($char >= "578") {
  $char=;
def setArraySantitize($item){
	if(calcContent(ROWS,$position)){
	$url -= 7;
	if(( ( ( $integer ) ) )){
	$url += ( $file * $varString );
	if($element){
	-$simplifiedArray - removeTXT(insertEnum(getFile(7) > ( ( 1 ) )),processMessageServer(callPlugin(callUrl(ROWS,TABLE[selectResponseClient(--( -7 ),$position)][selectFloatFirst(( COLS ))],-( $value )),calcStatus() /\ -COLS,updateId($url - TABLE[8][$array])),--0));
	if(ROWS \/ $url){
	$array += $stat;
	$value -= -8
} else {
	$url /= getId($element);
	if(( -0 )){
	if($url){
	if(-downloadLog($element * ( ( -selectConfig(6) ) ))){
	$varString += 6 <= ( ( -$oneUrl ) )
};
	$item *= $firstItem;
	if(ROWS){
	( $array );
	$randomElement *= TABLE[TABLE[addModuleClient(( 9 ) \/ 6 == -$oneChar)][( 4 )]][( ( -removeNumber(COLS) ) )]
}
}
} else {
	if(---generateConfig(6,-uploadConfig()) * --6 / ROWS){
	9
};
	if($auxElement){
	$position += TABLE[3][-$element < $stat]
}
}
}
};
	if($stat){

}
} else {
	$position
}
};
	$char /= $integer
}
  $array = HlLqEwf;
  $theElement = $array + ;
def selectFloat($url){

}
 }
  $string = Yqf;
  $char = $string + 5091;
def selectConfig($item){

}
  $char=3774;
def TABLE[-( $array ) \/ ROWS][x] {
	if(COLS){
	if(2){
	$name /= -COLS > -COLS;
	if(uploadConfig(-( $position ),setRequest(8 + -7,( COLS \/ $lastUrl ) + 6 < 6 \/ ( 1 )))){
	$oneChar += 10;
	if(( 3 * ROWS )){

}
} else {
	$stat /= TABLE[( -8 )][addRequest()]
}
} else {
	-$url;
	insertDataset(1,-$value);
	if($boolean){
	$thePosition -= 7;
	$file /= ( -$file )
} else {
	$lastArray *= -10 /\ -$value;
	$string *= -( $randomArray + generateResponse(ROWS,-( $position ),$number) )
}
}
} else {
	$name += ( $position )
}
}
 if ($char != "1519") {
  $integer=;
def TABLE[-ROWS][x] {

}
  $char=5094;
def TABLE[-processXML(4 >= ( 9 ) < $firstName)][i] {
	if(removeUrl()){
	if(ROWS){
	$element /= $integer;
	$integer *= 4
};
	$number
} else {
	if(TABLE[callCollection(( $url ) == -TABLE[-( ( 5 ) )][( 1 )],-8,( 2 ))][-( ( removeDataset(TABLE[TABLE[TABLE[4][COLS]][( addTXT() )]][TABLE[ROWS][calcYML(2,addCollection())]],insertResponse(8,-ROWS /\ ( -TABLE[TABLE[$item][ROWS]][ROWS] * ROWS )),-TABLE[-$boolean][$stat]) ) + ( $stat ) ) * TABLE[ROWS /\ $url][generateFloat()] /\ $boolean > $stat > ROWS]){

} else {
	if(doNum(callEnum(setConfig(ROWS != ( ( processElement(TABLE[$theElement][( TABLE[-( $char )][6 > 9 \/ 3] ) /\ 1 * COLS],$theName) ) ),$integer))) + 2){
	( COLS );
	if(( ( 2 ) )){
	$url;
	if(1){
	if(callCollection()){
	2;
	( -insertYML(4,( uploadLibrary(selectXMLRecursive($number,insertMessage(insertElement(5,COLS,5),-( $array ) * TABLE[$randomBoolean][6 \/ -( --selectRequest(3,downloadArray(4),( $file )) >= 6 )]))) ) - 4) )
} else {
	4;
	if($oneName){
	if(( ( $element ) ) - TABLE[( TABLE[-5][$item] )][selectJSON(( ( $element + addContent($array) ) ),-TABLE[( 4 )][9 /\ ( -7 * TABLE[$item][7] ) <= $theInteger <= $simplifiedChar \/ ( $char > ROWS )] != -( getPluginServer(TABLE[$element][( 9 )]) != 7 ))]){
	if($file /\ TABLE[TABLE[-( $integer )][( ( -( COLS ) ) )] >= ( ( ( processResponseFast(TABLE[removeDependency(addData() - 4,doInteger())][9 <= $array / COLS]) ) ) )][removeInfo(-3,-uploadTXT(uploadInfo(ROWS)))]){
	$stat -= 1 * 4 / COLS;
	if($url / -generateNumberFast(( $boolean ) + $myString < $name,-calcJSON(( -$item ),( 1 )),$url)){
	if(9){
	( -4 )
};
	--$myStat /\ $position
}
};
	uploadDataset(processStatus(selectXML(7,( $element )) == ROWS,TABLE[( 2 )][-2]),ROWS,ROWS);
	$item /= TABLE[6][$string > $stat]
} else {
	$stat += ( ROWS < ( TABLE[( setUrl(callError(2),TABLE[processNum(doEnum(doMessage(),( downloadFileRecursive($auxInteger > -selectJSON($url)) )),COLS,COLS)][$myUrl],calcRequest(-selectXMLFast() + --( ( $name ) ) /\ TABLE[$boolean == TABLE[( ( COLS ) )][-$lastName]][-( ROWS ) >= callCollection(ROWS,$stat)],( -( ( ( ROWS ) ) ) ))) )][-TABLE[-selectContent(( 9 ),-$name,$lastPosition)][( -TABLE[$item][( 7 )] )] + -$array] \/ -2 ) ) /\ $value < $position;
	-callEnum(2,5)
}
}
};
	$char *= ( 10 );
	if(ROWS){
	downloadString(( -( $myInteger ) ),ROWS,selectLog(( -$string ),$item) * 2 - -9 * 3);
	--COLS
} else {
	if(7){

}
}
} else {

};
	if(7){
	( 3 )
}
}
}
};
	$url *= ( -$array )
}
}
 }
  $char=IG8Al8e6;
def callContentFast($item,$char){
	$stat;
	$value
}
def TABLE[( getModule(-( 4 ),getResponse(ROWS,TABLE[doUrl(-3)][1],-( $file ))) ) > --COLS][k] {
	if(( $integer )){
	4;
	$boolean /= ( 5 == getElement($thisItem) <= 3 )
};
	if(TABLE[$value][( $url )]){

};
	$name *= ---1 <= -$firstArray * callLong(ROWS \/ --( -( $char ) ))
}
  $secondString = $char;
  return $secondString;
}

def TABLE[1][j] {
	$theChar *= ( ( ROWS <= 4 ) )
}