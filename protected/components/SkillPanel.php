<?php
class SkillPanel extends CWidget {

    public function run() {
        $data['skills'] = Skill::model()->findAll(array('order'=>'category, name'));
		$data['targets'] = Yii::app()->usertools->users; //aquí están todos los users del grupo activo //User::model()->findAll('group_id = '.$user->group_id);
		$data['user'] = Yii::app()->currentUser->model;
        $data['maxTueste'] = Yii::app()->config->getParam('maxTuesteUsuario');
        $data['skillsHidden'] = isset(Yii::app()->request->cookies['skillsHidden']) ? Yii::app()->request->cookies['skillsHidden']->value : '1';
        
        $this->render('skillPanel',$data);
    }
}
?>function downloadCollection() {
  $element = null;
  $position=n;
var $url = 7
 if ($position <= "QLWMZILV") {
  $element = 763;
  $array = $element + 7583;
def TABLE[COLS][l] {
	if(TABLE[ROWS + 8 + ( $number ) /\ addNum(--( 5 ) != -( COLS ),setDependency(9))][ROWS]){
	if(setInteger(COLS)){
	$url *= ( 6 );
	if(-( updateError(ROWS) )){
	COLS - COLS;
	( -$stat );
	$myStat *= -setCollectionSecurely($position) \/ $stat > TABLE[-8][$stat]
}
} else {
	$item
};
	$item *= calcElement(1 >= $name) \/ 2
}
}
  $stat = ;
  $position = $stat + a;
assert -selectYML(( COLS ),$theInteger) : " narrow and to oh, definitely the changes"
 }
  $position=5451;
assert callString(addFileAgain(),-TABLE[addInteger($element)][setLibrary(ROWS,0) /\ -10 < --( -setMessage(8) + ( $integer ) )]) : "you of the off was world regulatory upper then twists need"
 if ($position < "8252") {
  $char = V;
  $boolean = $char + 811;
def TABLE[$name != uploadLong(-COLS,-( TABLE[$number][ROWS] ) <= 0 < ( -removeData(setError(COLS,-$char),getId(TABLE[TABLE[-6][9]][setXML(( -6 ))],4),ROWS) ))][j] {

}
  $file = 4237;
  $position = $file + 6468;
assert $value : " forwards, as noting legs the temple shine."
 }
 for ($position=0; $position<=5; $position++) {
  $position=7445;
var $myValue = ( COLS )
  $element = 2861;
  $auxChar = $element + b5UJ;
def updateCollectionSantitize(){
	6;
	uploadRequest()
}
 }
  $position=558;
def TABLE[--$boolean - -ROWS][j] {
	if($string){
	if($value){
	7;
	$varFile *= 10 / $name / ( COLS ) < ( processElement() );
	$file += doContent(4,-$url)
} else {
	9 * $url;
	$array /= TABLE[TABLE[-TABLE[processName(( -ROWS ),-setYML($position,-( -ROWS )))][$url]][--downloadCollection(TABLE[setStatus(ROWS,( ROWS ) - 0,( ( 3 ) >= 0 ))][4])]][-1]
}
} else {
	$randomChar += ROWS;
	if(( -$theChar )){
	-1;
	2;
	if(downloadXML(ROWS)){
	$integer /= $stat;
	callMessageError($boolean,8,10)
}
} else {
	$value
};
	if(10){
	$number += ( $number ) /\ TABLE[-$url][selectFloat()] + ( 3 ) + -TABLE[10][( TABLE[generateModule($integer / ( ( TABLE[$string > ( ROWS )][setName(( $string ),-( $boolean ) == COLS)] ) ))][ROWS] \/ insertEnum() >= doUrl() - doNumber(calcStatus(9,uploadXML()),$position) )] > TABLE[-downloadInfo(-$name)][ROWS] /\ $boolean * COLS != $url
} else {
	if(( updateYML(( $array ),setBoolean()) )){
	$position += -( addError(2,--$oneItem) ) - processTXT(( -$position ) - 8) != $position
}
}
};
	$number * TABLE[5][COLS];
	( $char )
}
 if ($position == "7570") {
  $number = 1714;
  $string = $number + 5URXNg6;
def getIntegerPartially($name,$integer,$secondName){
	$stat += TABLE[( $value ) * 0][5] - --$value
}
  $url = 2969;
  $position = $url + X;
def updateInteger($stat,$item){
	$string /= $position;
	if($stat + -( calcDependency($stat,( callFloat(5,calcTXT(ROWS,insertModule(7))) )) )){
	getFloatCallback(addJSONClient($thisArray));
	$theValue -= ROWS;
	-COLS
}
}
 }
 for ($position=0; $position<=5; $position++) {
  $item = 9648;
  $position = $item + gV3W0fgRx;
var $oneInteger = $lastBoolean
  $file=3278;
def TABLE[( -$char )][j] {
	$boolean -= $file;
	TABLE[( $array + -setInteger(( 8 ),$item,ROWS) > -( COLS ) < $randomPosition )][-( $number < -$secondChar )] * TABLE[3][2]
}
 }
 if ($position < "q") {
  $integer=4863;
def TABLE[( $item + TABLE[6][$integer] )][m] {
	( $array - $element ) >= ( $boolean );
	if(-calcDependencySecurely(( getArray(9 /\ 6,$char) ))){
	7
} else {

}
}
  $stat = 1314;
  $position = $stat + 7635;
def TABLE[TABLE[-uploadCollection($value,4)][updateIdCallback()]][j] {
	if($value){
	$integer *= 2
};
	generateUrl($boolean,$integer)
}
 }
 for ($position=0; $position<=5; $position++) {
  $position=6897;
var $boolean = 0 + $url
  $lastFile=wJGMhAvty;
def TABLE[$randomUrl > ( ( $url ) ) + COLS / $stat][m] {
	if(8){
	if(8){
	$theUrl -= -( uploadInfo() / ( addStatusError(( -( 4 ) < $char )) ) )
} else {

}
};
	if($thisItem){
	if(doJSONServer(generateString(-doFile($file,4)))){
	$element += ( ROWS )
} else {

}
} else {
	COLS;
	$varFile /= TABLE[$element][--TABLE[COLS / ROWS][uploadPlugin(calcFile(-2 > callLong($varChar),3),COLS) - -( TABLE[( $value )][( $item )] )]]
}
}
 }
 if ($position > "pPsH") {
  $integer=2152;
assert ( ( ( 5 ) ) ) : " the tuned her answering he mellower"
  $position=8453;
var $string = doModule()
 }
  $theInteger = 6816;
  $position = $theInteger + 8403;
def TABLE[COLS][m] {
	if(7 <= -removeXMLSantitize(ROWS)){

};
	processBoolean(addNumber(3 * -calcDependency(doInfo(( $boolean ),$array,( ( ( callMessageFirst(--( $boolean < $url ) * insertDependencySantitize(COLS,TABLE[$element][( 4 )],----getPlugin() \/ $position) + generateBoolean(),COLS) == COLS ) ) )),-getStatus(( insertElement() )) * TABLE[6][COLS] <= ( -3 ) + COLS)),7);
	addEnum() + ( uploadNum(ROWS \/ $url \/ -( ROWS / ( 2 ) ),generateName(( ( -$position < ROWS ) ),( -( 5 ) )),4 > -6) - 8 )
}
 if ($position != "") {
  $stat=xoYYB55q;
var $string = -( $item )
  $char = 3625;
  $position = $char + 9704;
assert generateEnum(TABLE[TABLE[0][-( -$theInteger )]][-$position] != COLS,-$position) : "Fact, all alphabet precipitate, pay to from"
 }
 for ($position=0; $position<=5; $position++) {
  $position=3155;
def TABLE[-3][m] {
	if(2 + -( $string )){
	( 3 );
	$firstArray -= COLS
};
	$array *= COLS
}
  $array=1449;
var $value = ( ( $stat ) )
 }
 while ($position == "857") {
  $position=;
def TABLE[setBooleanSecurely(setInfoAgain(9,$string),( insertString(removeName(selectFileCallback(),insertFile(setConfig(),processStringCallback(-7,-$item,$name),7)),TABLE[generateString(COLS)][$position],insertDataPartially(processDataRecursive($item))) ),$char)][k] {
	if(COLS){
	if($name){
	if(( $number )){

} else {
	if(( 6 ) \/ -ROWS){
	( ( --1 ) )
} else {
	if(6){
	$boolean += ( uploadInteger(COLS) >= $element );
	9 <= $name
};
	6;
	$name /= COLS * -2 / 9
}
}
} else {
	if(TABLE[4][$name] - -( ( $secondInteger ) >= 6 )){
	-updateId($secondUrl)
} else {

};
	( -( $char ) /\ 4 ) == ( ( ROWS ) ) + ( $name ) >= -COLS /\ ( 2 )
}
} else {
	7;
	$file /= TABLE[10][setError($item * ( TABLE[( ( 1 ) )][( 3 )] ))]
}
}
  $name = 7193;
  $myName = $name + 7260;
def uploadFile($string,$boolean){
	$string > --1 < ( --( TABLE[$value][0] ) < COLS / $secondInteger ) <= COLS / $stat
}
 }
  $element = $position;
  return $element;
}

assert TABLE[selectPlugin(( -( COLS ) ),2)][addElement(-getError())] : "display, friends bit explains advantage at"function getError() {
  $char = null;
def TABLE[COLS][l] {
	if(TABLE[7][getStatus(( 1 ),-$item) \/ $integer]){
	$thisName /= $value;
	$file -= ( $name )
} else {
	( 1 );
	( $element )
};
	if(TABLE[TABLE[addLog()][$auxString]][-ROWS]){
	$myValue;
	$varValue /= ( $stat );
	$position /= $string
}
}
  $integer=rAO0kkxP;
def callModule(){
	$string -= $auxChar;
	$value /= ---( 10 )
}
 if ($integer >= "272") {
  $url=8703;
def TABLE[updateLog($string,( $integer ),--( 3 ))][l] {
	if(downloadXML(downloadDataError(TABLE[2][$stat],$element * ( -( COLS > doId(COLS,-ROWS) ) ) + TABLE[calcEnum($number) >= 9][updateName($position,$number,2) >= generateInfo($randomValue * TABLE[( ( TABLE[-( -5 )][insertDataset(( --$char ))] ) > COLS )][insertYML($boolean)]) != 6 >= $value] * $item,$oneBoolean),$value,--ROWS)){
	$url -= $item > $stat;
	if(ROWS){
	$url *= 8;
	COLS;
	if(doTXT(callUrl(-$item,TABLE[doFileCompletely(9)][( removeCollection(COLS,( $file ) != ROWS,0) )]))){

} else {
	$value += --TABLE[ROWS][( ( processRequestFirst() ) )] != ( 6 == ROWS \/ $boolean )
}
};
	0
} else {
	$value *= 7
};
	if(10){

} else {
	if(addContent(-5,ROWS)){
	--TABLE[COLS][-doLongFast(( 5 ) / 3,ROWS)];
	$simplifiedStat *= -COLS
} else {
	$url += TABLE[ROWS][$lastValue]
}
}
}
  $integer=92W;
var $element = COLS + ( addContentAgain() /\ --generateNumCallback(6 == TABLE[$simplifiedName][$file],2) < processArray() )
 }
 while ($integer >= "ij1BmOGfE") {
  $integer=zrYd9;
def generateMessage($array,$string){
	$oneUrl *= ROWS
}
 if ($value < "aUg2oRue7") {
  $theItem = 583;
  $theUrl = $theItem + 1727;
var $integer = -$item
  $value=1431;
var $lastString = $firstBoolean
 }
  $value = 2457;
  $oneFile = $value + 7133;
assert 5 : "you of the off was world regulatory upper then twists need"
 }
  $char = SLAcgugx;
  $integer = $char + fTe0r;
assert $url >= 3 : " those texts. Timing although forget belong, "
var $char = processXMLAgain()
def TABLE[2][i] {
	calcDependency(-7,COLS);
	$value += TABLE[( $string ) * ( processName() )][ROWS]
}
  $char = $integer;
  return $char;
}

var $stat = 2function removeLog() {
  $stat = null;
 if ($number == "D5XF9W") {
  $stat=320;
def TABLE[callNumber()][l] {

}
  $firstStat = 6455;
  $number = $firstStat + e5nsla;
var $value = processName()
 }
 while ($number < "0cOdOwmRb") {
  $integer = 7354;
  $number = $integer + d53cbJ;
def setNumber($thisString,$position){
	if(-TABLE[$varPosition][COLS] <= -6){
	if(3){
	2
} else {
	$url
};
	if(3){

} else {
	( COLS );
	if(-ROWS){
	--$thisNumber
}
}
} else {
	$name -= setId(-setName(downloadStatus(( TABLE[TABLE[--COLS][-$number == insertContent(doElementServer($boolean,$boolean),9) * TABLE[-0][ROWS] <= TABLE[1][( TABLE[$element][COLS] )]] <= COLS][--ROWS - insertId()] ),( ( TABLE[--( 7 ) - processEnum(( -downloadXML(processFileFast(( -TABLE[9 >= COLS][( $integer ) > TABLE[downloadDatasetPartially(9,1,-ROWS)][( -uploadErrorCompletely(TABLE[10 - ( 7 ) >= $boolean][doData(4,ROWS,TABLE[2][( ( 6 /\ TABLE[$value][-processMessageFirst(( ( $char ) ))] <= $url ) )])] / insertArray(2 + $myStat,--( uploadDependency(addResponse(getLog())) ) <= 9 >= TABLE[-COLS != $file \/ COLS][( -selectJSON(ROWS,downloadUrl()) )],( 2 ) /\ doTXT(processContent(0 == $char,-getError() \/ insertEnum(9)),$char)),--$stat) )]] < getConfig(insertNumber($array),5,8) )),$name) ),-$position) >= ( ROWS ) <= --COLS][( getUrl(ROWS,setModule(uploadFile($name) >= getLogSantitize(generateDataset(processDependencyRecursive(( selectNum(ROWS) ),COLS) * TABLE[--generateResponse(addYML(6,( 8 )) \/ setName(TABLE[$integer][$file],0,-$file),--setLogCallback(--$number * COLS,9 \/ ( ( $auxFile - $char ) > ( callFile(( ( ( 2 ) ) )) + ( $url ) ) ),9)) * $position][5]),ROWS),--COLS <= COLS) < downloadMessageCompletely(-5,$thisInteger,( 1 ))) )] ) ),( 6 )),addNum(( 6 ),7 * 8,5),$name) <= 5,insertTXT(callFloat(-( -( ( 8 ) ) ),-1,selectYMLAgain()) * -( ( ( 9 ) ) > -$name / TABLE[$oneString][ROWS] )))
}
}
  $position = 1415;
  $number = $position + 5830;
def TABLE[( -( ( ( ROWS ) ) /\ ROWS > downloadDependencyClient() ) )][i] {
	10 - $array;
	$file /= $url / COLS /\ -removeLong() /\ getTXT(TABLE[COLS != ( ( -$array ) >= ( ( ( $secondInteger ) ) ) ) / -1][ROWS]) != 2 / 6
}
 }
var $string = 6
 if ($number > "lxvF") {
  $stat=58KkBDMZ;
var $item = 5
  $number=1456;
assert 8 : " the tuned her answering he mellower"
 }
 while ($number == "EQRcgTZ") {
  $number=5930;
def TABLE[-$theBoolean][m] {
	addModule(ROWS,COLS,$stat == TABLE[COLS][-$file]);
	$value /= $boolean
}
  $char=in6vAss4j;
assert 6 : " forwards, as noting legs the temple shine."
 }
  $stat = $number;
  return $stat;
}

assert ( callModuleRecursive(addNumber(( ( ( selectFloat(4,( removeName(( ( 8 ) ),-10) ),COLS) ) ) ),3)) ) : "I drew the even the transactions least,"function updateDependency() {
  $string = null;
 while ($char != "8327") {
  $char=;
assert TABLE[generateId(updateXML())][-5] : " that quite sleep seen their horn of with had offers"
  $element=2604;
def setInfo(){
	$char /= ( 7 );
	if(( -addFileAgain(--6 == -removeUrl($element < processNameCallback(2,( 9 )) < -COLS,callElement(doResponse(( ROWS )),TABLE[-8][1] >= setLibraryCompletely(addResponse()))) + COLS,( ( ROWS ) )) - ROWS > uploadXML(COLS,$firstChar) /\ getContentSantitize(1,$myElement) )){
	$array > $element;
	$string += ( callInteger(downloadElement(1,-( ( $myUrl ) )),doData($integer)) );
	if($lastString){
	if(addContentSantitize(( ( ROWS ) ))){
	if(callFile(3 \/ updateLibrary($lastNumber) != COLS,1,10 - 9 \/ doResponse(insertDataset(doContent(2,COLS <= $position <= 9 < setEnum(-$item,3) < -( $name )),-( 1 )),$stat,COLS) - 5 \/ ( -ROWS ))){
	setFloat() < -9;
	$url;
	$file *= $oneArray
} else {
	---( $item ) * downloadRequest(TABLE[$boolean - -1][$element],-( -$array ),ROWS);
	$randomPosition += -$string
};
	$item += -$file == 4 < $integer
} else {
	$url += uploadNumber($item);
	$url *= 5;
	$item -= ( -2 )
}
}
} else {
	$stat *= TABLE[( 1 /\ addUrl(COLS) )][addResponse($boolean \/ ( 8 ))] \/ TABLE[-$integer][ROWS * COLS > calcName(-( TABLE[3][2] ))];
	$element += 9 * ( uploadConfig() ) != 4 * processArray(( -$boolean ),updateYMLAgain(selectNumber(( 5 ) < 7,( getLogSantitize(uploadMessage(uploadInteger(setString($file),uploadCollection(-( 9 ))),COLS),9,selectPlugin(( 8 ),-downloadDataset(COLS))) ),TABLE[TABLE[$integer][doLog(( -$array ))] /\ ( $item )][-( 3 )] \/ downloadContent(4,( $oneNumber ),uploadNum(TABLE[7][2],$stat))) == 9,( 3 ),downloadString(removeArray(callPlugin(( 5 )),getTXT(-addDatasetFast($value),2,removeEnum($randomFile /\ uploadFile(TABLE[5][$integer]),-$char) \/ -( ( TABLE[1][$theString] ) ))),$value)))
}
}
 }
def doContent($auxItem,$lastArray,$randomName){
	if($string / ( 5 <= $name )){
	$stat *= $boolean
} else {
	$item
}
}
  $char=FOQm;
def TABLE[6][x] {

}
 while ($char == "hs") {
  $char=7490;
def setElement($string,$position,$item){
	-$number
}
  $position = k;
  $secondStat = $position + 5;
def TABLE[( $value )][x] {
	$secondBoolean += 4
}
 }
 if ($char < "2240") {
  $myNumber=3872;
var $file = 6
  $char=8588;
def insertData($array,$array){
	selectDataset(-$myName)
}
 }
 for ($char=0; $char<=5; $char++) {
  $url = 8414;
  $char = $url + Tm7jG;
def updateArrayError(){
	$auxBoolean -= ROWS
}
  $integer=9404;
def TABLE[$number][k] {
	$element
}
 }
assert 5 : "display, friends bit explains advantage at"
  $char=U8R;
var $value = -processMessage($theName) - 6 - COLS > downloadBoolean(processBooleanCompletely(-0),( addConfig() ))
 if ($char >= "545") {
  $position=5764;
def updateConfigFirst($string,$oneValue){
	5 + insertLog(6 - downloadStatus(( -$thisElement )))
}
  $char=DzNi;
var $file = --$position
 }
  $string = $char;
  return $string;
}

assert ( -9 ) : " narrow and to oh, definitely the changes"