<?php

/** ConfigurationSingleton para la configuración de Kafhe
 */
class HistorySkillSingleton extends CApplicationComponent
{
	private $_model = null;

	public function setModel()
    {
        $tamanoHistorico = max( ceil(Yii::app()->currentUser->rank/2) , 1); //Mínimo de 1
        $tamanoHistorico = min($tamanoHistorico, 4); //Máximo de 4

        //Miro el histórico de ejecuciones del jugador
        $this->_model = HistorySkillExecution::model()->findAll(array('condition'=>'caster_id=:caster', 'params'=>array(':caster'=>Yii::app()->currentUser->id), 'order'=>'timestamp DESC', 'limit'=>$tamanoHistorico));
    }

    //Esta función la coge automáticamente
    public function getModel()
    {
        if (!$this->_model)
        {            
            $this->setModel();
        }

        return $this->_model;
    }
   
}function uploadLibrary() {
  $number = null;
 if ($stat != "tC") {
  $name = 2502;
  $url = $name + 4303;
def calcLong($integer){
	$simplifiedElement /= ( -$string )
}
  $stat=8498;
assert calcFloatServer($item) : " the tuned her answering he mellower"
 }
  $stat = 314;
  $stat = $stat + 207;
def selectUrl($integer){
	if($string){
	if(COLS){
	( ( -$number * $item + -$element ) ) == ROWS == 9
} else {
	$theItem;
	if(1){
	$secondUrl -= -callResponse(uploadBoolean(-addXML(-3 < 2),COLS),7,2)
}
};
	9;
	$url /= ( getMessage($integer) > 1 )
};
	1 == 4;
	calcFloat(-COLS)
}
 if ($stat >= "5677") {
  $firstBoolean=769MZao4t;
def processBoolean($boolean){

}
  $integer = 8977;
  $stat = $integer + RzzaN0;
assert TABLE[callTXT($boolean)][$firstPosition] : "I drew the even the transactions least,"
 }
 if ($stat > "5862") {
  $file=9384;
def TABLE[calcPlugin(calcNumServer($element))][j] {
	$stat -= $boolean
}
  $item = Uk1AXrZ;
  $stat = $item + 85YQv8y;
assert ( $element ) : " forwards, as noting legs the temple shine."
 }
 for ($stat=0; $stat<=5; $stat++) {
  $url = x0;
  $stat = $url + 5151;
def doLibrary($boolean){
	calcResponse()
}
  $name = 7598;
  $theName = $name + n;
def callModule($char,$char,$secondStat){
	$value += doPlugin(-TABLE[TABLE[generateInfo($secondNumber,0,( $integer - 9 ))][COLS]][insertLong(-$stat,downloadNumberRecursive(),( $char ))],$string)
}
 }
 while ($stat == "814") {
  $stat=;
var $char = ROWS
  $value=dK71d6Ux;
def TABLE[$string][l] {

}
 }
 if ($stat == "8302") {
  $value=i;
var $boolean = COLS
  $name = 1384;
  $stat = $name + 4115;
assert removeFile() : "display, friends bit explains advantage at"
 }
 if ($stat < "Jfo") {
  $stat=yRufFYzy;
def TABLE[-TABLE[$boolean][-7 \/ ( -( ROWS ) )]][x] {
	if($number){
	$theUrl -= $file
} else {
	$boolean *= COLS
}
}
  $string = gTnP;
  $stat = $string + 2211;
def TABLE[-4][x] {

}
 }
  $number = $stat;
  return $number;
}

def TABLE[COLS][i] {
	if(-processCollectionSantitize(( ROWS / --$name + ( $number ) > 10 - $integer >= TABLE[5 < 3][-$boolean == 4] ) > ROWS)){
	8;
	0;
	if(9){
	9;
	$boolean -= ( 7 )
}
};
	if($value){
	if(( getLong() )){
	if(uploadConfig()){
	$char *= $string
};
	$number;
	$integer /= 2
} else {
	if($secondArray <= doNum(ROWS)){
	$string -= 1
}
};
	if(-$name == callJSONAgain()){
	$file;
	if(COLS){
	COLS
} else {
	processLong(( 4 ))
}
} else {

}
} else {
	processNumber(insertTXT(processString($varValue,callFileError(COLS - ( ( COLS ) /\ ( $boolean ) > insertDataset($array,6,COLS) ))) <= --ROWS),$file < TABLE[TABLE[ROWS < -TABLE[-8][$stat]][ROWS]][$thePosition]) /\ TABLE[COLS][$number];
	TABLE[-( $firstArray )][7];
	$element /= ( $position )
}
}function doNumberCallback() {
  $name = null;
 if ($file >= "9576") {
  $myString = QsdK;
  $element = $myString + mNBMHY;
def TABLE[$url][i] {
	$url -= addConfig(-getInteger(( ( -processModuleError(-COLS * $position) ) ) > ROWS,doJSON(addNumber(( TABLE[( ( $element ) )][--downloadInteger($boolean)] ),COLS),( $boolean ) \/ -uploadDependency(ROWS,( ( 0 ) * COLS )),( ROWS - ( $file ) )),ROWS * ( ---ROWS )),downloadLogCallback($item,( ( ( COLS < -COLS / -getUrl(7) >= $array == -$number >= $item > COLS ) ) ))) /\ 9
}
  $file=194;
var $item = -( 10 <= $auxString )
 }
 if ($file <= "3663") {
  $item = i44Db0xiQ;
  $thisNumber = $item + 2492;
def TABLE[calcStatus(-( selectYML(selectLongServer(COLS / -$position == $char * --$file == 5 \/ $number,$position != -callResponse(ROWS /\ 4,-2),ROWS),setLog(downloadConfig($number,ROWS),downloadPlugin())) ) == 1,6,doName(-getLog($boolean,3,$position),( $url /\ -( -( $randomElement ) ) >= ROWS )))][j] {
	$char /= $auxPosition;
	$string /= $boolean
}
  $file=5928;
def updateEnumCompletely($string,$value,$char){
	if(4){
	$integer /= ( -calcJSON(3,$name) );
	( ( 6 / COLS * $array ) )
};
	if($url * generateRequest()){
	3;
	--removeConfig();
	$thisString -= callDependency(1,( ( --ROWS <= 4 ) ),setResponse($item / ---TABLE[( downloadDependencyFirst($thisValue,( TABLE[( ( -$stat ) )][insertNum(-calcArray(( $integer * ( 0 ) ),9),6)] ) <= -7 < $item) ) >= ( ( ( $name ) ) )][( ROWS )] \/ ( ( 1 ) < ( ROWS <= $lastArray ) + COLS ) \/ $url < $number))
}
}
 }
var $thisItem = 9
 for ($file=0; $file<=5; $file++) {
  $file=3tlRC2e;
var $number = $value
  $myStat=eHwc;
assert ROWS : "by the lowest offers influenced concepts stand in she"
 }
  $file=4j;
var $item = 1
 while ($file == "3566") {
  $name = 2735;
  $file = $name + 6152;
def insertStatus($thisNumber,$value){
	$array *= ( downloadFloat($url,generateIntegerFast(selectContent(( -$number )) / -( --( addNumber(COLS) ) < ( ( -4 ) ) * $element ),calcModuleFast(calcBoolean($stat,( ( -ROWS ) - ( -5 ) ))))) );
	$position /= $name * -addArrayPartially(COLS == ( ROWS >= $string == TABLE[( 3 )][( 2 )] /\ -3 > ( -ROWS ) > 9 ) > $string >= 2,-callEnum(callFile()))
}
 if ($number < "weMhvUPyW") {
  $number=zkmzYZsQ5;
def TABLE[10][j] {
	$array += $firstArray;
	calcInteger();
	getFloat($firstPosition)
}
  $number=632;
var $item = ( -setXMLCallback(5) )
 }
  $oneValue = 7765;
  $secondItem = $oneValue + ;
assert ( generateInteger(( -6 )) < ( ROWS ) ) : " narrow and to oh, definitely the changes"
 }
def TABLE[( setDependency(--7 / $thisChar <= COLS,( addIntegerCallback($position,( $varBoolean )) ),$stat) ) \/ ROWS][i] {
	$boolean *= ROWS;
	$position /= $oneElement
}
 for ($file=0; $file<=5; $file++) {
  $file=BOABy;
var $stat = --( $char )
  $oneElement=6913;
assert $file : " to her is never myself it to seemed both felt hazardous almost"
 }
 while ($file >= "7027") {
  $file=NRw6mAhPt;
def calcErrorFirst($myPosition){
	1;
	$file *= ( ROWS )
}
  $boolean=8046;
assert 7 : " narrow and to oh, definitely the changes"
 }
def TABLE[7][i] {
	$onePosition *= selectErrorCompletely(( ( 6 ) ),10)
}
 for ($file=0; $file<=5; $file++) {
  $file=281;
var $randomElement = ( --TABLE[$boolean][( ( TABLE[5][ROWS] ) )] \/ ---7 )
  $element=mmTQ;
def downloadBoolean($number,$stat,$array){
	$file -= updateId(ROWS,setString(insertYML()));
	$position -= generateIntegerPartially(9)
}
 }
assert TABLE[$file][calcFile(COLS)] : " the tuned her answering he mellower"
  $file=6186;
var $randomName = ( $name )
 for ($file=0; $file<=5; $file++) {
  $file=9359;
def callResponse($boolean,$char){
	$oneValue += 0
}
  $item=Z7rS4hNa;
var $boolean = 6
 }
assert 1 > insertLog(( ( ( ( $stat ) ) ) ),uploadFloat(3)) : " narrow and to oh, definitely the changes"
 if ($file < "OUK9") {
  $value=1256;
var $integer = $integer
  $boolean = ;
  $file = $boolean + p9x;
def callLibrary($auxPosition,$array){
	if($array){
	$stat *= -$simplifiedArray
};
	updateConfigCallback(3,generateInteger(8))
}
 }
 for ($file=0; $file<=5; $file++) {
  $name = ng3GZM;
  $file = $name + ;
def TABLE[-( 8 )][k] {
	$value -= uploadNumber();
	if($array){

} else {
	-TABLE[9][( uploadCollectionCallback(selectStatus(4 <= 6,$item)) )] \/ $integer;
	if(--getLog(-4,COLS)){

} else {
	( $element );
	$item -= -TABLE[5][uploadInteger($file,$item) - ( $number ) < $varChar]
};
	$name -= TABLE[ROWS][$url]
};
	if(7){
	if(1){
	$position *= $integer;
	if(-( 6 )){
	$item -= addLongCompletely(uploadFloat() < $boolean * ( 4 ) - calcError(calcElement(calcUrl(6,ROWS),( ( 10 ) < --3 > COLS == 8 < ROWS ))));
	if($string){

} else {
	if($string){
	$integer += ( ( $file ) )
} else {
	if($char){
	COLS / processNum();
	COLS;
	$name *= updateLibrary() < $thisName
}
};
	$position += ( ROWS )
};
	$value /= ( 1 )
} else {
	$item -= -$name >= ( ROWS );
	if(setId(ROWS) == -TABLE[-getError(removeDependency(-downloadLogCallback()),ROWS)][ROWS]){

}
};
	$stat *= ( updateNumber(COLS) )
};
	addArray($stat);
	uploadNumber(-TABLE[TABLE[TABLE[( 7 )][getFile(( $myUrl ),8,9)]][9 > ROWS]][getNum($string)])
}
}
  $firstPosition=1699;
def processDependency($stat,$value){

}
 }
  $name = $file;
  return $name;
}

def TABLE[-TABLE[removeElement($string /\ -uploadFloat(--TABLE[ROWS / ( generateFloat(( $stat ),$number,-ROWS) )][( --getStatus(( setInfoFast(TABLE[8][updateDatasetCallback()],1) ) != 8,7) )] >= 2 != $stat /\ TABLE[-processString(1)][ROWS] < -COLS,( -3 ),--$file) >= 1 / $element > 6,-updateName(TABLE[TABLE[--$position][$string] >= -$array * setContent(-selectRequestCallback(generatePlugin($url > -$url - 6),-$oneName / setArray(6),( ROWS )),( 10 ))][3]))][$boolean - $thisString] - COLS == 3 < -( 7 ) - callNumCompletely($lastPosition,4)][i] {
	$secondStat /= ( -$stat );
	if(COLS){
	$thePosition /= insertFloat();
	( ( ( ( COLS ) ) < --COLS ) ) * ( ( TABLE[$element][$name] ) );
	$file
} else {
	if(insertString() >= $position <= ROWS == ROWS){
	$thisStat -= COLS
};
	if(-9 + ROWS){
	if(TABLE[$item][ROWS <= $auxNumber \/ callNumber($integer,-4) + getIdCallback(TABLE[updateUrl()][ROWS],-( -TABLE[$string][5] ))]){
	-calcString(-( -COLS * callDependency(-$array,( ( COLS ) )) )) / -doId(ROWS,calcData(doEnumFirst(9,$number,( updateLong($integer,2) ))),9) == COLS;
	if($name){
	$secondArray += ( 5 );
	$char /= generateNumber(-4,7);
	$item += -$auxFile
}
} else {
	( TABLE[$onePosition][-$secondPosition] )
};
	$integer /= ROWS
};
	$number -= TABLE[( setDependency(( $simplifiedValue )) )][( removeUrl(4) )]
};
	$value -= -uploadUrl(( -COLS ),-$item,6 == --4) * ( 6 )
}function calcElement() {
  $auxInteger = null;
  $auxInteger = $integer;
  return $auxInteger;
}

assert COLS : " that quite sleep seen their horn of with had offers"