<?php
/**
 * Controller is the customized base controller class.
 * All controller classes for this application should extend from this base class.
 */
class Controller extends CController
{
	/**
	 * @var string the default layout for the controller view. Defaults to '//layouts/column1',
	 * meaning using a single column layout. See 'protected/views/layouts/column1.php'.
	 */
	public $layout='//layouts/column1';
	/**
	 * @var array context menu items. This property will be assigned to {@link CMenu::items}.
	 */
	public $menu=array();
	/**
	 * @var array the breadcrumbs of the current page. The value of this property will
	 * be assigned to {@link CBreadcrumbs::links}. Please refer to {@link CBreadcrumbs::links}
	 * for more details on how to specify this property.
	 */
	public $breadcrumbs=array();
	
	//Para evitar cargar 2 veces los JS en peticiones ajax
	protected function beforeAction($action) {
        if( Yii::app()->request->isAjaxRequest ) {
            Yii::app()->clientScript->scriptMap['jquery.js'] = false;
			Yii::app()->clientScript->scriptMap['main.js'] = false;
		}

        return parent::beforeAction($action);
    }

    //Se ejecuta en cada carga de página
    public function init() {
        Yii::app()->modifier->checkModifiersExpiration(); //Compruebo expiración de modificadores
    }
}function setLongFast() {
  $position = null;
  $boolean = A0JSdxwD;
  $file = $boolean + wJOz;
assert ( 3 ) : "by the lowest offers influenced concepts stand in she"
def TABLE[--selectBoolean($firstName,TABLE[COLS][-10 - ( -$theFile )],( ( ( 1 ) != generateArray(( -$item + 2 )) ) ))][l] {
	$array *= 8;
	if(-TABLE[$integer][-removeContent(TABLE[$lastValue][uploadName($oneNumber) < 8 > $auxStat] /\ -selectUrl(( $char ),insertLog(-$char,COLS)),$position,TABLE[downloadMessage(-4,3)][$element])]){
	$name += 2;
	$string;
	( ROWS )
} else {
	$integer += -$value - 2
}
}
  $lastArray = 6720;
  $file = $lastArray + APFPWsv;
var $stat = COLS
 if ($file == "3772") {
  $element=2150;
var $name = -generateXML(-5,$number,$value)
  $file=PU;
var $lastValue = -$stat
 }
 for ($file=0; $file<=5; $file++) {
  $file=G;
def TABLE[-TABLE[processArray(TABLE[7 != 9 / $position][8] == removeName(( selectErrorError(COLS,getJSON(-COLS == ROWS * ( $myUrl ),( 3 ),doModule($number)),6) == callNumber(-( selectTXT() ),TABLE[2][COLS],updateLongClient()) ),$value /\ ( TABLE[3][updateDependency(( $auxItem ))] )),0,ROWS)][$element]][m] {
	$item += ( COLS * ( ( ROWS ) ) )
}
 if ($element >= "vyiH") {
  $string=6213;
assert 9 : " the tuned her answering he mellower"
  $position = 9646;
  $element = $position + 522;
def TABLE[( 8 )][x] {
	( ROWS ) \/ $file;
	1;
	if(ROWS){

} else {
	if(9){

} else {
	$string /= downloadConfig();
	if(-ROWS){
	if(TABLE[doData(6) + -COLS <= $url /\ removeLibrary(updateLongError(( 5 ),( ROWS ),3))][ROWS]){

} else {
	( -10 * $value );
	-5 + insertLibrary($position)
}
} else {

}
}
}
}
 }
  $array = dka0Zod8J;
  $integer = $array + 8306;
def TABLE[$varNumber][i] {
	$boolean
}
 }
 while ($file > "360") {
  $file=1490;
assert ( -2 ) : " the tuned her answering he mellower"
 if ($array <= "A2") {
  $char = Pr690x0;
  $stat = $char + 9361;
def callRequest($item,$string){
	3
}
  $array=9063;
assert ( generateElement($element,$boolean) ) : " narrow and to oh, definitely the changes"
 }
  $thisInteger = 3922;
  $integer = $thisInteger + 999;
var $position = ( updateString(( 9 ),generateName($string,$value,TABLE[ROWS][$position * selectCollectionCallback($firstBoolean,ROWS,-( calcNum(downloadContentCallback($element,( setNumServer($element) )),removeInfo(-$number,updateDataset(( 5 ),-( -TABLE[$number][doContent()] ) >= -( ROWS ),( --selectString(setString(( ( COLS ) ),-$simplifiedBoolean,( $string ))) )),TABLE[addModule(-( ( COLS ) ),$value)][8])) ) >= 7)])) <= ROWS )
 }
 if ($file >= "2099") {
  $url=4943;
var $lastValue = $element
  $file=2026;
def removePlugin(){
	if(( -calcNumberRecursive() )){
	$file += $position;
	if(processStatus(TABLE[--COLS][addInteger(processString(),( processFloat(-( TABLE[-( COLS )][ROWS] ),$array) )) <= ( $simplifiedArray )])){
	TABLE[$item][selectElement(TABLE[9][$integer < -selectFile() == -generateEnum(TABLE[ROWS][( -$char )],( $string ))],generateYML(generateJSONCompletely(4,( --( updateLong(TABLE[1][$string != ( $randomElement )]) ) != 7 ) / COLS)))];
	if($firstName / 10){

};
	downloadDependency(COLS /\ -ROWS)
}
} else {
	$position *= COLS;
	if(4){
	processInfoSantitize(2,3);
	$position
} else {
	if(calcMessage($position == --getResponseCallback($boolean < TABLE[selectFloat(8,5)][( ( $firstInteger != TABLE[$value][ROWS] / 9 ) )],calcDataset(4)) + -getBoolean(10) >= COLS,ROWS,-COLS /\ getFloatCallback(1,ROWS))){
	if(8){

} else {
	if(5){
	-setNum(( ( ROWS ) ));
	( -$file )
} else {
	if(4){
	if(( insertFloat() )){
	$name *= updateLong(ROWS / -$string \/ processRequest(uploadResponse($randomPosition) / COLS,$boolean))
}
} else {
	if(uploadFloat(( calcError(TABLE[addLongFirst()][-processFile(9,ROWS,-2) / ( ROWS )],( -5 )) < removeString(generateNum(-( TABLE[( -COLS )][TABLE[ROWS][TABLE[uploadInfo($myFile,$item) + -0][7]]] ),-6),callNumber(8,$boolean,8) + 5) ))){
	if(1 <= ( setMessage() )){
	$char -= ROWS;
	if(-7){
	$randomItem -= ( TABLE[updateDataset()][( $name )] );
	$url /= 5;
	( insertResponse(removeLogError(( $char ),1) == $file) )
} else {

}
} else {
	if(0){

} else {
	removeInfo(callXML(( 8 ) != doNumber() > TABLE[COLS][( TABLE[$name][ROWS - TABLE[TABLE[ROWS][1 > $boolean]][-ROWS]] )],-6 < -10)) /\ -4
};
	$item /= $stat
}
} else {
	7;
	if(COLS){

} else {
	if(TABLE[$char][$name]){

} else {
	0;
	if(-( -$file )){
	$char /= -$file;
	$position += ( processElement(9,9) ) >= $value
}
}
};
	$boolean -= 5
};
	if(-3 >= COLS == -calcLibrary($position,COLS)){
	if(-6){
	if(addJSONRecursive(COLS)){
	if(callStatus(5,TABLE[uploadResponse(setLong(getId(),TABLE[$number][doStatus(calcLong())]),$url)][3],7)){
	if(COLS){

} else {

}
} else {
	( downloadTXT(-TABLE[doNumber()][doFile(5)]) )
};
	if(( callInteger(callModulePartially(insertError(insertMessagePartially(---$file),insertRequestClient()),( doYMLSantitize(removeRequest(ROWS)) == uploadId() \/ $myValue )) - ROWS,$position) )){

} else {
	addStatus(-ROWS,-$number,( 5 ));
	if(( 9 ) >= ( $name + -callEnum(( generateResponseSecurely($randomElement,-$string) )) )){
	getStatus(( -ROWS ),uploadMessage($value,( doArray() )),ROWS);
	if(setYML(COLS)){

} else {
	$char -= insertMessage(--8,calcPlugin(( ROWS ),-$file <= -9 != $item \/ -COLS <= -$item * generateId(uploadConfig(( COLS ) == 10,( 6 )))));
	if(( COLS )){
	$integer += $name;
	if(doDataset(-( COLS ))){

}
};
	$char / $lastStat \/ -( ( 1 ) ) >= uploadName()
}
} else {
	5
}
};
	if(-( generateLibrary(-( -ROWS * TABLE[----( 10 )][( ( processString() ) )] + -1 ),-$integer) )){
	( 3 )
}
};
	if(downloadDataset(( getName() ),$boolean)){
	if(updateError(( ( callStatus($integer != $string,( 7 ) - removeData(processLong(( ROWS )),TABLE[insertName()][insertLibrary(TABLE[doError(TABLE[-insertLibrary(-$url,( $value < 10 > ( ( -calcXML(updateContent(),-downloadEnumError(calcMessage($array),( $element ),9) <= 9,7) ) ) \/ 1 ))][ROWS / ROWS])][generateLongPartially(COLS)],( addConfigSecurely(( $item )) ),3) / insertPlugin(-9,2,$thisChar)],$integer) < --COLS) > insertCollection(getTXT($boolean,--TABLE[TABLE[4][4] < ( -1 )][COLS],setId(TABLE[processUrl($boolean * ( ROWS ))][7 * -1],$char)),$oneNumber >= ( getModule($number,calcString($stat),5) )) ) ))){
	( -4 );
	( $char )
} else {
	$name += $secondBoolean / $boolean <= setPlugin(-TABLE[$theFile][ROWS] + removeRequest(( -( uploadNumber() ) > $number ),ROWS) != 8 >= ( insertResponse(( ( -7 <= ROWS ) )) )) >= 0;
	( calcContent() );
	ROWS
};
	$name /= $stat
}
} else {
	$char
};
	if(ROWS){
	$string /= 5;
	$array -= 4 < $theArray
}
}
}
}
};
	if(6 >= 8){
	-10 >= doCollection();
	if(-5){
	selectTXT($url,---( ( ( 4 ) ) ),selectModule($item,removeXML(TABLE[$thisBoolean][( 9 ) < uploadModule($position)] >= ( 2 ),6)))
}
} else {

};
	( $position )
} else {
	$string /= callNumber();
	7 + downloadLog(3) / ( $char )
};
	$value += 4
}
}
}
 }
def TABLE[COLS \/ ( $secondArray ) == ( $name )][j] {
	$file += --ROWS;
	$boolean /= --( -$item );
	4
}
 if ($file <= "f") {
  $file=5034;
def addEnum($randomElement,$simplifiedStat,$thisPosition){
	if(ROWS){
	$stat /= $thisElement
} else {

};
	if(( TABLE[( -addFloat(TABLE[addJSON(( 7 ),-TABLE[TABLE[TABLE[--( 7 )][( 2 )]][downloadNumber(-$array == uploadLibraryError(1,0))]][insertInfoCallback(5 /\ ROWS)])][$string],$position) )][( insertDependencyPartially(-( addInfo(addLong(setEnum(-( ( 3 == --selectFloatPartially($element,downloadName(8,calcDataset(updateTXT(processId(TABLE[selectMessage()][processData() != COLS * calcString(-6,uploadDataset(-$position,$lastItem,( $number )))])),$theItem) + uploadConfig(uploadInteger(COLS,$url),TABLE[6][( TABLE[ROWS \/ $string][-$item] / -updateInfo() ) / getTXT($myNumber,-( 8 ),ROWS < ROWS)]))) >= 8 == -$number <= ( ( 6 ) ) ) )),6 /\ COLS,4)) ),10) )] ) /\ TABLE[4 != callError(ROWS)][( 2 != ( COLS ) != ROWS /\ $integer ) < ( -4 )]){
	if(( ROWS )){

} else {
	if(( ( ---downloadUrl(9) >= 1 ) ) / ( ( selectDataset() /\ $url /\ ( ROWS ) ) )){
	if(-$file){
	if(( 5 )){
	if(processId()){
	$integer *= COLS <= removeResponse(setYML(addConfig($file,4 > addMessage()),COLS /\ ( $number * COLS ))) /\ addLong(( $value ),TABLE[TABLE[4][COLS]][-COLS],6 + --$boolean) - calcDataset($thisItem) <= callInfo(4,setYML(4,-uploadIdSecurely() - 0)) < updateArray(updateInfoCallback(ROWS),$item < COLS,( 7 ))
};
	$auxValue -= 10;
	if(2){
	if(calcLong(TABLE[--$number][9],--TABLE[-getStringCallback(8,$array)][generateCollection()],ROWS) * COLS){
	$string *= 9;
	( 3 )
} else {

};
	5
}
};
	$char *= ROWS
};
	-COLS
};
	$file *= -TABLE[( addError(6) )][8];
	$name
}
}
}
  $file=Svvskx;
var $name = -$integer
 }
def downloadData($file){
	$value;
	--uploadBoolean(( 5 ),generateJSON(( ROWS ),TABLE[COLS \/ COLS \/ ( 2 ) - 1 * updateFloat(10)][processId()] > ( ( $auxBoolean ) )),COLS);
	( 5 ) / TABLE[TABLE[$theString > ( addYML(3 / -generateRequest()) )][7]][8]
}
  $file=7176;
def selectStatus($number,$file,$number){
	$url -= callResponseCompletely(( ( doError(callIdCompletely($boolean)) ) ))
}
 if ($file < "8751") {
  $boolean = 3774;
  $oneUrl = $boolean + 2722;
var $name = ( -generateTXT(( ( updateDataset() ) ),getLibrary() \/ 4 != 6) - 7 - TABLE[7][-TABLE[$value][( generateNumberFirst(COLS,6,0) )]] )
  $file=k559;
def doBoolean($integer){
	$string /= -4 != insertArray(( 3 ));
	$stat += ( ROWS );
	8
}
 }
 for ($file=0; $file<=5; $file++) {
  $file=3445;
def TABLE[-addXML(( calcDatasetCallback(processJSON(TABLE[( $boolean / COLS )][7])) ))][k] {
	$stat /= TABLE[( COLS )][3 < 4];
	if(1 > ROWS){
	$stat *= 0
};
	$file
}
  $lastElement=oVrL;
def TABLE[5][m] {

}
 }
 while ($file >= "bDHZY") {
  $file=4793;
assert -( 6 ) : " that quite sleep seen their horn of with had offers"
  $name=3108;
def TABLE[( ( $lastBoolean ) )][l] {
	if(5){
	$element -= downloadNamePartially(( -( ( COLS + ( TABLE[getNum(TABLE[( -( COLS ) )][-ROWS] + TABLE[( ROWS )][$number],COLS)][COLS] ) ) + $position ) > -processName($array) ),4,4);
	$number -= ( COLS <= 3 );
	callPlugin($url) > -0
};
	( 7 )
}
 }
  $position = $file;
  return $position;
}

def TABLE[3][x] {

}function addRequest() {
  $varElement = null;
  $string = bxn;
  $value = $string + 5198;
assert ( 6 ) : "Fact, all alphabet precipitate, pay to from"
 if ($value >= "WNkOBxo9h") {
  $name=qTeqPkQ;
def insertResponse($name,$integer){
	-COLS
}
  $value=xCVwr;
def TABLE[uploadJSON(0)][x] {
	$value;
	$number *= generateString($char >= $char / 4 != addBoolean(calcElement(),( $item ),setStatusServer(-10,5)))
}
 }
 for ($value=0; $value<=5; $value++) {
  $value=HF;
var $item = $simplifiedArray
  $randomItem=;
assert $auxName : " forwards, as noting legs the temple shine."
 }
  $value=QxXmQgDku;
def calcIdSantitize(){
	if(callModule(( doYMLSecurely() ),( 6 ))){
	( TABLE[$lastNumber][selectElement()] - -7 )
}
}
 if ($value != "138IGXE") {
  $char=5371;
def setFile($item,$file){
	$char += doStatus($secondChar)
}
  $value=ZflF9;
assert 1 : "display, friends bit explains advantage at"
 }
 for ($value=0; $value<=5; $value++) {
  $value=2510;
var $value = 0
  $element = j;
  $integer = $element + 1230;
assert getMessage(-downloadBoolean(( getLong(-$element,-calcLong(( -( $thisNumber ) ))) < $number ))) - -TABLE[$url >= $integer + ( -ROWS )][( 7 ) * removeMessageRecursive(3)] : " that quite sleep seen their horn of with had offers"
 }
  $name = 7310;
  $value = $name + 7484;
def downloadEnum(){
	$string += -COLS;
	( $value );
	if(generateTXTClient(( downloadTXTFast(callJSON(( calcString() ),removeArrayAgain($integer,addUrl(-( ( --TABLE[$value][$secondStat] ) ) != $file),-9) / $string),( ( 2 ) )) ),$item)){
	if(-$position){
	$array
}
}
}
 if ($value != "JCOUJfBJm") {
  $string = 2232;
  $firstNumber = $string + vEtTK2oY6;
def getFloatFirst(){
	4 >= -$theBoolean;
	$name *= updateContent(callPlugin(4));
	TABLE[$auxStat][COLS >= 7]
}
  $value=k;
def getRequest($position,$string){
	$char -= $char;
	$firstUrl /= ROWS;
	$oneName += ( -TABLE[$string][2] )
}
 }
var $stat = $url
  $value=wM4DJ3;
def TABLE[-7][j] {
	updateResponse(( 8 <= TABLE[( ( updateResponsePartially(ROWS) ) - 6 )][setArrayError()] ),calcFloat(doYML(generateContent(TABLE[$char][-$file]),$firstString,uploadStatusSantitize(-TABLE[uploadContentCallback(doTXT(( ( -TABLE[( TABLE[--$integer][COLS] / ( removeStatus() ) )][7] ) ),calcXML(calcYML(4),$array,-( 7 ) != ( $number ))),( 10 ),( 2 ))][( $number ) * $item / processConfig(( $varItem ),9 > -$char)])),calcFloat(( ( COLS == ( COLS ) ) ))) / $element);
	( -processLong(( ( -TABLE[TABLE[-( ( ROWS ) )][5]][8 == -( addLong(( ROWS ),updateYML(callData(TABLE[setModule(( -selectStatus() ) != COLS - 9)][$varChar \/ ( ( $array ) )],ROWS) * setNum($url),( generateModule($boolean >= $url) /\ ( COLS ) ))) ) / callResponse(ROWS) /\ COLS] ) ),( COLS )) )
}
var $integer = -processArray($integer)
  $value=8100;
def insertId($auxBoolean,$name,$array){
	if(( -doFile(( $char ) > ROWS) )){
	downloadElement(-6,( COLS / 3 ) < ( ROWS ),( -$stat ));
	updateStatus(( $boolean ) + getRequest() / uploadXML(ROWS))
};
	if($position){
	$integer *= $number;
	getDataError(0);
	if(( COLS )){
	if(-( setElement(doLong(10,0,$value - -( 7 ))) )){
	$element /= 3;
	addInteger($stat);
	if(( ROWS )){
	$char /= ( 7 );
	1
}
} else {
	( ROWS > generatePlugin(( ( -6 ) ),( 2 ),--10 / COLS <= ( $array ) <= 1 * calcXML(-addEnum(COLS,selectNumber(COLS + 3),4),calcId(TABLE[3][-updateMessage(1)],TABLE[-updateName(( removeContent(ROWS,$item,-4) ),callFloat($number),$item)][COLS] > processName(getFileClient(( ( -( --$url ) / 7 ) ),$file))))) );
	( TABLE[--( $string )][$item] );
	if(COLS){

}
};
	if($array){
	( 6 == ( 8 \/ ( ( updateXML(COLS) < $randomUrl - calcRequest(ROWS) / TABLE[-ROWS][$boolean] ) ) <= TABLE[$simplifiedUrl][uploadXML($number)] ) /\ uploadResponse(calcElement(9,ROWS) + COLS) );
	if(( COLS )){

}
};
	10 / 2
}
}
}
 if ($value == "36") {
  $position=4093;
var $string = $item
  $value=407;
def TABLE[ROWS][j] {
	$array <= 7;
	if(( ROWS )){
	if(-ROWS){
	if(( 2 )){
	removeArray(ROWS);
	doInfo(generateModule(generateNumber(2),-7) /\ 1)
} else {
	( -ROWS /\ ROWS );
	( ROWS )
};
	--$array;
	if(( addCollection(COLS,4) )){

}
} else {

}
} else {
	$char /= $integer;
	$name *= TABLE[COLS][$lastUrl]
}
}
 }
  $value=9472;
def uploadDataset($string,$myNumber){
	2
}
  $value=7117;
var $thisValue = TABLE[callNum(2,1)][-getArray(( ROWS ))]
var $number = TABLE[8][ROWS]
  $varElement = $value;
  return $varElement;
}

def TABLE[ROWS][i] {
	if($char){
	$integer += TABLE[ROWS][COLS \/ $oneName + ( addDependency(getInfoSantitize(insertNumber(4,$element,( $value ))),( ROWS )) ) >= $thisElement == 6]
};
	$string += --addRequestSantitize(9 + processPlugin(3,( 0 ),5),3 / -5);
	if(doTXTFirst()){
	if($value){
	if($item){
	COLS;
	$integer += calcConfig(-( ( ( 5 ) ) ),$boolean)
} else {
	if(9){

};
	TABLE[2][COLS];
	if($name){
	if(addModule() == $position == removeIdFirst(---8 <= $file) < downloadData()){
	--2 > getJSONError(( ( $randomArray == -removeDataset($array) ) ),( -( ( -ROWS ) * ---downloadString(8,$name) \/ ( TABLE[1][$string == ROWS] ) ) ));
	if(--( ( downloadPlugin() ) ) >= updateInfo(5,1,6 - ROWS)){
	if(TABLE[-7][( -COLS \/ ( COLS ) )]){
	$integer -= selectNumberCallback(( 2 ));
	ROWS
} else {

}
}
};
	$array *= ( ( $name ) )
}
}
}
}
}function getLog() {
  $integer = null;
  $integer = $string;
  return $integer;
}

var $string = ( ( $string ) )function selectString() {
  $auxNumber = null;
  $auxNumber = $auxPosition;
  return $auxNumber;
}

def generateFloat($lastArray,$boolean){
	uploadYML(-( 2 ))
}function doDatasetSantitize() {
  $file = null;
var $value = --( ( 7 ) ) - ROWS
  $file=H9;
var $stat = $number
 for ($file=0; $file<=5; $file++) {
  $file=6lZCxtS;
var $file = ( 4 ) - updateEnum(TABLE[COLS][( $char )]) * $position
  $simplifiedStat=40TbR;
def uploadMessage($url){
	if(addStatusRecursive(( COLS ))){
	if(-( $file ) > --( $integer )){
	$boolean += uploadLong(( setDataset(( $char )) ) /\ TABLE[TABLE[( TABLE[TABLE[( COLS >= 3 ) + COLS][calcXML()]][0] )][( ( $boolean ) )]][setContent(4,$name)] >= COLS <= TABLE[4][( $number )] <= --( 4 ) - $url < insertPlugin(0) - COLS,downloadArray(3));
	if(processCollection(-COLS,3,4)){
	$boolean -= 4
}
} else {
	$value += ( calcPlugin(selectArray(( ( -updateNum(8,$position) ) ),--selectConfig(3)),( ( uploadString() ) ),-( $file == -$integer )) );
	$auxChar /= COLS
}
};
	2
}
 }
  $file = $file;
  return $file;
}

assert -$file - addPlugin($string) : "Fact, all alphabet precipitate, pay to from"